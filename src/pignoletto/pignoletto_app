server {
    listen 5000;
    server_name 0.0.0.0;
    client_max_body_size 0;

    location / {
        include uwsgi_params;
        uwsgi_pass unix:pignoletto_app.sock;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
    }
}
